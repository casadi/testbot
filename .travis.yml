env: 
  global: 
    - secure: bOPhFqXlBfwm73ac6BMSba2UR3vwnYVDqyXeAs/5SxrW2hW7/V/MmYLQL3h3BpOYfPBa1ganEAKYrd9K6HWJN16MHyROBEbFpZmZ5DFbK/pUjYaJp+8giSytxWBzEjo2f129azETNgvzjANbpAe0+uYaxupUPvpfQkPPrII+2Jc=
  matrix:
    - recipe=coinhsl
    - recipe=snopt7
python:
- "2.7_with_system_site_packages"
compiler: 
  - gcc
before_install:
  - sudo apt-get update -q
  - DEBIAN_FRONTEND=noninteractive DEBIAN_PRIORITY=critical sudo apt-get -q -y -o "Dpkg::Options::=--force-confdef" -o "Dpkg::Options::=--force-confold" dist-upgrade
  - sudo apt-get -f install ia32-libs-multiarch -y; sudo apt-get -f install
  - sudo apt-get install libc6:i386 libncurses5:i386 libstdc++6:i386
  - recipes/setup.sh
  - restricted/GetMac.linux32.old
  - restricted/GetMac.linux32.new
  - ls
  - sudo apt-get update -qq
  - sudo apt-get install -y gcc g++ gfortran
install: 
  - recipes/$recipe.sh
script:
  - echo "All is fine"
  
